version: '2'

services:
    nginx:
        image: nginx:1.13.6
        ports:
            - 8000:80
        volumes:
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf

    php:
        build: ./docker/php/
        volumes:
            - .:/var/www/html