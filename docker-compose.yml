version: '2'

services:
    application:
        build: ./docker/php/
        volumes:
            - .:/var/www/html
        links: 
            - redis
        ports: 
            - 8000:80

    redis:
        image: redis:3.2.10
        ports:
            - 6379:6379